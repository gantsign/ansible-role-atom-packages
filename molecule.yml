---
ansible:
  playbook: tests/test.yml
  requirements_file: tests/requirements.yml

docker:
  containers:
    - name: ansible-role-atom-packages-ubuntu-xenial
      image: ubuntu
      image_version: '16.04'
