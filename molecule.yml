---
docker:
  containers:

      - name: ansible-role-atom-packages-01
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1

      - name: ansible-role-atom-packages-02
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1